{
  "installCommand": "cd Desktop/book && npm install",
  "builds": [
    {
      "src": "Desktop/book/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "build"
      }
    }
  ]
}